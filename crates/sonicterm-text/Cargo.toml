[package]
name = "sonicterm-text"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "SonicTerm Terminal — headless text layer: shaping, swash rasterization, glyph atlas, per-row cache"

[lints]
workspace = true

[dependencies]
sonicterm-types.workspace = true
# T10/T13/T14: cosmic-text is gone — sonicterm-text's surviving
# modules (glyph_atlas, resample, row_glyph_cache, shape, shape_wt)
# don't reference cosmic-text after the chrome migration.
swash.workspace = true
fontdb.workspace = true
bytemuck.workspace = true
lru = "0.16"
tracing.workspace = true
fast_image_resize = { version = "5.1", default-features = false }
